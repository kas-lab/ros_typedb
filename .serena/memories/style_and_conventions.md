Python code follows ROS 2 ament_flake8 and ament_pep257 checks. Use 4-space indentation, snake_case for modules/functions, PascalCase for classes, descriptive ROS parameter names, and keep console entry point names aligned with setup.py. Preserve the package layout and avoid mixing generated ROS interfaces with Python runtime modules. Prefer Serena symbolic navigation for non-trivial Python code exploration and keep edits scoped to the requested package/behavior.